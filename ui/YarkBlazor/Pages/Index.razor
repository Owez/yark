@page "/"
@inject NavigationManager NavManager

@* This page is just a redirect depending on current login state *@

@code {
    protected override void OnInitialized()
    {
        @* TODO: in the future actually implement, no state as of time of writing *@
        NavManager.NavigateTo("/start");
    }
}